---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"h2">;

const { class: className, ...props } = Astro.props;
---

<h2
  class:list={[
    "text-xl font-semibold text-neutral-800 sm:text-2xl lg:text-3xl dark:text-neutral-200",
    className,
  ]}
  {...props}
>
  <slot />
</h2>
